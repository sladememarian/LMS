java:
  stage: test
  tags:
    - local
  script:
    - ./gradlew clean test -Dtestfx.headless=true -Dtestfx.robot=glass -Dprism.order=sw -Dprism.text=t2k -Djava.awt.headless=true
  artifacts:
    when: always
    paths:
      - "build/test-results/test/TEST-*.xml"
      - "build/reports/tests/test"