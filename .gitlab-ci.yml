stages:
  - test

java:
  stage: test
  image: cirrusci/openjdk:17
  tags:
    - local
  script:
    - xvfb-run -a -s "-screen 0 1920x1080x24" ./gradlew clean test
  artifacts:
    when: always
    reports:
      junit: build/test-results/test/TEST-*.xml
    paths:
      - build/reports/tests/test